# 🔧 Переработка генератора правила Друзья: многозначные действия, проверка МИКСА, расширенные диапазоны

## 📋 Описание изменений

Полная переработка `FriendsExampleGenerator` согласно техническому заданию для корректной работы с многозначными числами и правильной реализации правила "Друзья".

## ✨ Основные изменения

### 1. Целевой разряд = самый старший
- **Двузначные** (digitCount=2): целевой разряд = 1 (десятки)
- **Трехзначные** (digitCount=3): целевой разряд = 2 (сотни)
- **Четырехзначные** (digitCount=4): целевой разряд = 3 (тысячи)
- Формула: `targetPosition = digitCount - 1`

### 2. Правило Просто = однонаправленность движения бусин
- **Сложение**: только ДОБАВЛЕНИЕ бусин (U: 0→1, L: увеличение)
- **Вычитание**: только УБИРАНИЕ бусин (U: 1→0, L: уменьшение)
- **НЕЛЬЗЯ** смешивать добавление и убирание в одном действии!

Примеры:
- Состояние 2 (U=0, L=2):
  - `+1` ✅ (Просто: L: 2→3)
  - `+2` ✅ (Просто: L: 2→4)
  - `+3` ❌ (Братья: нужно U:0→1, L:2→0 - смешанное!)
  - `+5` ✅ (Просто: U:0→1, L:2→2 - только добавить верхнюю)
  - `+6` ✅ (Просто: U:0→1, L:2→3 - добавить обе)

### 3. Многозначные действия для Friends
- Генерация действий типа **+10, +11, +12, ... +19** для двузначных
- Цифра Friends применяется к **целевому разряду** (старшему)
- Остальные разряды подбираются так, чтобы работали по правилу Просто

Пример: состояние [9, 2], цифра Friends = 1
- ✅ **+10**: десятки 9+1 (Друзья), единицы 2+0 (Просто)
- ✅ **+11**: десятки 9+1 (Друзья), единицы 2+1 (Просто: L+1)
- ✅ **+12**: десятки 9+1 (Друзья), единицы 2+2 (Просто: L+2)
- ❌ **+13**: десятки 9+1 (Друзья), единицы 2+3 (Братья: МИКС!)
- ✅ **+15**: десятки 9+1 (Друзья), единицы 2+5 (Просто: U:0→1)

### 4. Проверка МИКСА
Добавлен метод `_hasMix()` для проверки многозначных действий:
- Проверяет **каждый разряд** отдельно
- Целевой разряд: должен использовать Друзья (переход через 10)
- Остальные разряды: должны работать по Просто (без Братьев)
- Если хотя бы один не-целевой разряд требует Братья → **МИКС → действие НЕДОСТУПНО**

### 5. Расширенные диапазоны
Промежуточные результаты могут расти:
- **Двузначные**: 0-999 (может стать трехзначным!)
- **Трехзначные**: 0-9999 (может стать четырехзначным!)
- Формула: `maxValue = 10^(digitCount+1) - 1`

Пример:
```
Настройки: двузначные
Начало: [5, 5] = 55
+45 → [10, 0] = 100 ← трехзначное! Это нормально!
+234 → [334] ← промежуточный результат в пределах 0-999 ✓
```

### 6. Точное количество шагов
- Параметр `stepsCount` вместо диапазона `minSteps-maxSteps`
- Генератор стремится создать **РОВНО** указанное количество шагов
- Поддержка любого количества: 5, 50, 500, 5000...

### 7. Количество Friends = "сколько возможно"
- Минимум **1** действие Friends (обязательно)
- Максимум - сколько удастся сгенерировать естественным образом
- Равномерное распределение Friends по всему примеру (не кучкой)
- Стратегия: после каждого Friends вставлять 2-3 действия Просто

### 8. Разнообразие и рандомизация
- Трекинг использования цифр Friends (если выбрано несколько)
- Избегание повторения последней использованной цифры
- Чередование действий Просто (разные цифры)
- Случайная позиция Friends в последовательности

## 🔧 Технические детали

### Новые методы:
- `_numberToDigits()` - разбить число на цифры по разрядам
- `_digitsToNumber()` - собрать число из цифр
- `_canApplySimpleDirect()` - проверка многозначного действия по Просто
- `_isFriendsAction()` - проверка является ли действие Friends
- `_hasMix()` - проверка МИКСА (Друзья + Братья)
- `_canAddTenToTarget()` - можно ли добавить +10 к целевому разряду
- `_canSubtractTenFromTarget()` - можно ли убрать -10 из целевого разряда
- `_tryGenerateFriendAddition()` - попытка сгенерировать +action Friends
- `_tryGenerateFriendSubtraction()` - попытка сгенерировать -action Friends

### Переработанные методы:
- `_canPlusDirect()` - добавлена проверка однонаправленности
- `_canMinusDirect()` - добавлена проверка однонаправленности
- `_generateAttempt()` - новая стратегия генерации с многозначными действиями
- `_generateFriendAction()` - генерация многозначных Friends действий
- `_generateSimpleAction()` - генерация многозначных Просто действий
- `_applyAction()` - применение многозначных действий
- `_checkOverflow()` - проверка расширенных диапазонов
- `_fallbackExample()` - улучшенный fallback с гарантированными Friends

## 📝 Обновленная документация

Добавлены подробные комментарии в начало файла:
- Описание целевого разряда для каждой разрядности
- Формулы расширенных диапазонов
- Правило Просто с примерами
- Запрет МИКСА с объяснением

## 🧪 Тестирование

Добавлены тестовые файлы:
- `test_friends_generator.js` - комплексное тестирование
- `test_simple.js` - простой тест

## ⚠️ Известные ограничения

### Ограничение 1: Совместимость friendDigits с brothersActive=false

При `brothersActive = false` (верхняя бусина запрещена) не все цифры Friends могут быть использованы:

**Совместимые цифры** (не требуют изменения верхней бусины):
- **Цифра 9** (friend=1): состояния 1,2,3,4 → вычитание 1 не меняет верхнюю бусину ✅
- **Цифра 8** (friend=2): состояния 2,3,4 → вычитание 2 не меняет верхнюю бусину ✅
- **Цифра 7** (friend=3): состояния 3,4 → вычитание 3 не меняет верхнюю бусину ✅
- **Цифра 6** (friend=4): состояние 4 → вычитание 4 не меняет верхнюю бусину ✅

**Несовместимые цифры** (требуют изменения верхней бусины):
- **Цифры 1-5**: формулы требуют вычитание 9,8,7,6,5 из состояний с верхней бусиной ❌

**Решение**: Fallback генератор автоматически подбирает совместимую цифру Friends или документирует невозможность генерации.

### Ограничение 2: Точное количество шагов

При `brothersActive = false` основной генератор может испытывать трудности с достижением точного количества шагов из-за строгих ограничений на использование верхней бусины. В таких случаях используется улучшенный fallback генератор, который:
- Подбирает совместимую цифру Friends (предпочтение: 9, 8, 7, 6)
- Прямо подготавливает целевой разряд к нужному состоянию
- Гарантированно создает примеры с минимум 1 действием Friends
- Заполняет оставшиеся шаги разнообразными простыми действиями

## 🎯 Соответствие ТЗ

- ✅ Целевой разряд = самый старший
- ✅ Правило Просто = однонаправленность
- ✅ Многозначные действия
- ✅ Проверка МИКСА
- ✅ Расширенные диапазоны
- ✅ Точное количество шагов
- ✅ Количество Friends = минимум 1, максимум по возможности
- ✅ Разнообразие и рандомизация
- ✅ Валидация примеров

## 🔥 Критические исправления

### Коммит 0c6eed5: Исправлена случайность примеров

**Проблема:** Все сгенерированные примеры были **одинаковыми** после Friends действия!

**Причина:** Фиксированный массив `simpleActions = [1, 2, 3, 4, 1, 2]` был одинаковым для всех примеров. После Friends состояние всегда одинаковое (например, [0, 1]), поэтому с фиксированным массивом получались одинаковые остальные шаги.

**Пример проблемы:**
- Пример 1: `... → +1(F) → +1 +2 -3 +4` = **12** ❌
- Пример 2: `... → +1(F) → +1 +2 -3 +4` = **12** ❌ (одинаковый!)
- Пример 3: `... → +1(F) → +1 +2 -3 +4` = **12** ❌ (одинаковый!)

**Решение:**
1. **Рандомный массив** действий для каждого примера:
   ```javascript
   for (let i = 0; i < 10; i++) {
     simpleActions.push(Math.floor(Math.random() * 4) + 1);
   }
   ```

2. **Случайный выбор** порядка попыток (сложение/вычитание):
   ```javascript
   const tryAdditionFirst = Math.random() < 0.5;
   ```

**Теперь работает:**
- Пример 1: `... → +1(F) → +1 +2 -1 +1` = **13** ✅
- Пример 2: `... → +1(F) → +4 -1 +1 -4` = **10** ✅ (разный!)
- Пример 3: `... → +1(F) → +2 +1 -3 +3` = **13** ✅ (разный!)
- Пример 4: `... → +1(F) → +2 -1 +3 -4` = **10** ✅ (разный!)
- Пример 5: `... → +1(F) → +2 +1 -3 +2` = **12** ✅ (разный!)

### Коммит 79fdf29: Исправлена логика правила Просто

**Проблема:** Методы `_canPlusDirect` и `_canMinusDirect` неправильно проверяли `brothersActive`, из-за чего переход **4→9 (+5)** отвергался как "требующий Братья".

**На самом деле:** 4→9 (+5) - это **чистое Просто**:
- U: 0→1 (добавить) ✓
- L: 4→4 (не трогать) ✓
- Только ДОБАВЛЕНИЕ = однонаправленное движение!

**Решение:** Убрана проверка `brothersActive` из методов проверки правила Просто. Теперь они проверяют **ТОЛЬКО физику абакуса** (однонаправленность), а не настройки генератора.

### Коммит 153ce9c: Обходные пути через 9/0

**Проблема:** Некоторые переходы невозможны по правилу Просто напрямую!

**Критический случай: 4→8 (для friendDigit=2)**
- Состояние 4: U=0, L=4 (четыре нижние бусины)
- Состояние 8: U=1, L=3 (верхняя + три нижние)
- Прямой путь 4+4→8: нужно добавить верхнюю (+1) И убрать нижнюю (-1)
- Это **МИКС** (добавление + убирание одновременно) → **запрещено!**

**Решение:** Обходные пути через промежуточные состояния:

1. **Обходной путь ВВЕРХ (через 9):**
   ```
   4 → 9 (+5, Просто: только добавить верхнюю)
   9 → 8 (-1, Просто: только убрать нижнюю)
   ```

2. **Обходной путь ВНИЗ (через 0):**
   ```
   6 → 0 (-6, Просто: только убрать)
   0 → 3 (+3, Просто: только добавить)
   ```

**Теперь работает:**
```
+4(начало) → +5(обход через 9) → -1(→8) → +2(Друзья: +10-8) → ... = 10
```

### Коммит ae9c5b1: Поддержка вычитания в подготовке

**Проблема:** Fallback не мог подготовить единицы, когда нужно УМЕНЬШИТЬ значение (например, для friendDigit=9 нужно состояние 1, а случайный старт дал 3).

**Пример ошибки:**
- friendDigit=9, нужно состояние единиц = 1
- Случайный старт: +3 (состояние = 3)
- Старая логика: пытается только ДОБАВИТЬ, но 3 > 1!
- Результат: ❌ застряли на 3, не можем применить Friends

**Решение:** Двунаправленная подготовка единиц:
- Если current < target: **ДОБАВЛЕНИЕ** (+9, +8, +7, ... +1)
- Если current > target: **ВЫЧИТАНИЕ** (-9, -8, -7, ... -1)
- Если current == target: готово!

**Теперь работает:**
```
+2(начало) → -1(вычитание!) → +9(Друзья: +10-1) → ... = 14
```

### Коммит 9e472b6: Упрощенная агрессивная стратегия

**Проблема:** Fallback был слишком осторожный и не мог подготовить единицы к нужному состоянию.

**Решение:** Упрощенная и эффективная стратегия:

1. **Случайное начало** - первое действие (+1 до +4)
2. **Агрессивная подготовка** - пробуем МАКСИМАЛЬНО возможное:
   - Цикл: +9, +8, +7, +6, +5, +4, +3, +2, +1
   - Используем ВСЁ что работает по правилу Просто
   - Пример: `3 → +6 → 9` (одним шагом вместо +1+1+1+1+1+1!)
3. **Friends действие** - когда готовы
4. **Остальные шаги** - разнообразно заполняем

### Коммит 3d24cde: Только однозначные действия

**Проблема:** При настройках "однозначные числа" генератор создавал ДВУЗНАЧНЫЕ действия (+90, +10).

**Решение:** Fallback переработан для использования **ТОЛЬКО ОДНОЗНАЧНЫХ** действий с применением Friends к единицам (не к целевому разряду) с переносом в десятки.

## 📊 Примеры работы

**Fallback с Братьями (friendDigit=1, 7 шагов):**
```
+3(начало) → +6(агрессивно!) → +1(Друзья: +10-9) → +1 +2 -3 +4 = 14
```

**Fallback без Братьев (friendDigit=9, 7 шагов):**
```
+2(начало) → -1(вычитание!) → +9(Друзья: +10-1) → +1 +2 -3 +4 = 14
```

**Fallback с обходным путем (friendDigit=2, 7 шагов):**
```
+4(начало) → +5(обход через 9) → -1(→8) → +2(Друзья: +10-8) → +1 +2 -3 = 10
```

**Основной генератор (многозначные настройки):**
```
+22 +100(Д) +15 +203(Д) +31 +104(Д) +8 = 483
```

## 🔗 Связанные файлы

- `ext/core/FriendsExampleGenerator.js` - основной файл генератора
- `test_friends_generator.js` - комплексные тесты (все сценарии)
- `test_simple.js` - тест с братьями (brothersActive=true, friendDigit=1)
- `test_without_brothers.js` - тест без братьев (brothersActive=false, friendDigit=9)
- `test_digit2.js` - тест для двузначных (friendDigit=2, обходной путь)
- `test_detour.js` - специальный тест для проверки обходного пути
- `test_randomness.js` - тест проверки уникальности примеров
